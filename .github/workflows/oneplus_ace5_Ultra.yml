name: oneplus_ace5_Ultra
on:
  workflow_dispatch:
    inputs:
      KERNEL_SUFFIX:
        description: '内核名称修改(可改中文和emoji)'
        required: true
        default: '-android15-oki-xiaoxiaow'
      KERNEL_TIME:
        description: "内核构建日期更改(默认为原厂)"
        required: true
        default: 'Tue Jul  1 19:48:18 UTC 2025'
      enable_feature_x:
        description: "是否启用kpm"
        required: true
        default: false
        type: boolean
      enable_feature_y:
        description: "是否启用lz4kd"
        required: true
        default: true
        type: boolean
      enable_feature_z:
        description: "是否添加风驰驱动"
        required: true
        default: true
        type: boolean

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"
      CCACHE_DIR: /home/runner/.ccache  
      CCACHE_MAXSIZE: 8G
    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          
      - name: Configure Git
        run: |
          git config --global user.name "Futaba"
          git config --global user.email "3199167602@qq.com"

      - name: Install dependencies
        run: |
          sudo apt update && sudo apt upgrade -y
          sudo apt install -y python3 git curl ccache libelf-dev build-essential flex bison libssl-dev

      - name: Restore ccache
        uses: actions/cache@v3
        with:
          path: /home/runner/.ccache
          key: ccache-${{ runner.os }}-${{ github.ref }}
          restore-keys: |
            ccache-${{ runner.os }}-

      - name: Install repo tool
        run: |
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/repo
          chmod a+x ~/repo
          sudo mv ~/repo /usr/local/bin/repo

      - name: Initialize repo and sync
        run: |
          mkdir kernel_workspace && cd kernel_workspace
          repo init -u https://github.com/HanKuCha/kernel_manifest.git -b refs/heads/oneplus/sm8750 -m oneplus_ace5_Ultra.xml --depth=1
          repo --trace sync -c -j$(nproc --all) --no-tags
          rm -f kernel_platform/common/android/abi_gki_protected_exports_* || echo "No protected exports!"
          rm -f kernel_platform/msm-kernel/android/abi_gki_protected_exports_* || echo "No protected exports!"
         
      - name: Check kernel structure
        run: |
          cd kernel_workspace/kernel_platform
          echo "检查内核目录结构:"
          find . -name "Makefile" -type f | head -10
          ls -la
          echo "common目录内容:"
          ls -la common/
          echo "检查是否有kernel目录:"
          ls -la common/ | grep kernel || echo "没有kernel目录"
         
      - name: Set up SukiSU (修复版)
        run: |
          cd kernel_workspace/kernel_platform
          echo "当前目录: $(pwd)"
          echo "目录内容:"
          ls -la
          
          # 首先检查内核目录结构
          if [ -d "common" ]; then
            echo "找到common目录"
            cd common
          elif [ -d "kernel" ]; then
            echo "找到kernel目录"
            cd kernel
          else
            echo "❌ 错误：找不到内核目录"
            ls -la
            exit 1
          fi
          
          # 检查Makefile是否存在
          if [ ! -f "Makefile" ]; then
            echo "❌ 错误：在当前目录找不到Makefile"
            echo "当前目录内容:"
            ls -la
            exit 1
          fi
          
          echo "✅ 找到Makefile，继续设置SukiSU"
          cd ..
          
          # 下载并运行SukiSU设置脚本
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s susfs-main
          
          # 检查KernelSU目录是否创建成功
          if [ ! -d "KernelSU" ]; then
            echo "❌ 错误：KernelSU目录未创建"
            exit 1
          fi
          
          cd KernelSU
          echo "⬇️ 下载KernelSU设置工具..."
          curl -L -o sukisu_setup.bin https://github.com/Xiaomichael/OnePlus-Actions/raw/Build/script/sukisu_setup.bin
          chmod +x sukisu_setup.bin
          ./sukisu_setup.bin
          echo "✅ SukiSU Ultra配置完成"
        
      - name: Set up susfs
        run: |
          cd kernel_workspace
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android15-6.6 || echo "susfs4ksu克隆完成"
          git clone https://github.com/SukiSU-Ultra/SukiSU_patch.git || echo "SukiSU_patch克隆完成"

          cd kernel_platform        
          
          # 复制susfs文件
          if [ -d "../susfs4ksu" ]; then
            cp ../susfs4ksu/kernel_patches/50_add_susfs_in_gki-android15-6.6.patch ./common/ || echo "复制patch文件失败"
            cp ../susfs4ksu/kernel_patches/fs/* ./common/fs/ || echo "复制fs文件失败"
            cp ../susfs4ksu/kernel_patches/include/linux/* ./common/include/linux/ || echo "复制include文件失败"
          fi
          
          # lz4k
          if [ -d "../SukiSU_patch" ]; then
            cp -r ../SukiSU_patch/other/zram/lz4k/include/linux/* ./common/include/linux || echo "复制lz4k include失败"
            cp -r ../SukiSU_patch/other/zram/lz4k/lib/* ./common/lib || echo "复制lz4k lib失败"
            cp -r ../SukiSU_patch/other/zram/lz4k/crypto/* ./common/crypto || echo "复制lz4k crypto失败"
            cp -r ../SukiSU_patch/other/zram/lz4k_oplus ./common/lib/ || echo "复制lz4k_oplus失败"
          fi
          
          # Apply patches
          cd ./common
          if [ -f "50_add_susfs_in_gki-android15-6.6.patch" ]; then
            patch -p1 < 50_add_susfs_in_gki-android15-6.6.patch || echo "应用susfs patch失败"
          fi
          
          if [ -f "../../SukiSU_patch/hooks/syscall_hooks.patch" ]; then
            cp ../../SukiSU_patch/hooks/syscall_hooks.patch ./
            patch -p1 -F 3 < syscall_hooks.patch || echo "应用syscall hooks patch失败"
          fi
          echo "✅ SUSFS配置完成"
      
      - name: Apply HMBird GKI patch
        run: |
          cd kernel_workspace/kernel_platform/common/drivers
          cat << 'EOF' > hmbird_patch.c
          #include <linux/init.h>
          #include <linux/module.h>
          #include <linux/of.h>
          #include <linux/slab.h>
          #include <linux/string.h>

          static int __init hmbird_patch_init(void)
          {
              struct device_node *ver_np;
              const char *type;
              int ret;

              ver_np = of_find_node_by_path("/soc/oplus,hmbird/version_type");
              if (!ver_np) {
                    pr_info("hmbird_patch: version_type node not found\n");
                    return 0;
              }

              ret = of_property_read_string(ver_np, "type", &type);
              if (ret) {
                    pr_info("hmbird_patch: type property not found\n");
                    of_node_put(ver_np);
                    return 0;
              }

              if (strcmp(type, "HMBIRD_OGKI")) {
                    of_node_put(ver_np);
                    return 0;
              }

              struct property *prop = of_find_property(ver_np, "type", NULL);
              if (prop) {
                    struct property *new_prop = kmalloc(sizeof(*prop), GFP_KERNEL);
                    if (!new_prop) {
                        pr_info("hmbird_patch: kmalloc for new_prop failed\n");
                        of_node_put(ver_np);
                        return 0;
              }
              memcpy(new_prop, prop, sizeof(*prop));
              new_prop->value = kmalloc(strlen("HMBIRD_GKI") + 1, GFP_KERNEL);
              if (!new_prop->value) {
                    pr_info("hmbird_patch: kmalloc for new_prop->value failed\n");
                    kfree(new_prop);
                    of_node_put(ver_np);
                    return 0;
              }
              strcpy(new_prop->value, "HMBIRD_GKI");
              new_prop->length = strlen("HMBIRD_GKI") + 1;

              if (of_remove_property(ver_np, prop) != 0) {
                  pr_info("hmbird_patch: of_remove_property failed\n");
                  return 0;
              }
              if (of_add_property(ver_np, new_prop) !=0) {
                  pr_info("hmbird_patch: of_add_property failed\n");
                  return 0;
              }
              pr_info("hmbird_patch: success from HMBIRD_OGKI to HMBIRD_GKI\n");
          }
          else {
              pr_info("hmbird_patch: type property structure not found\n");
          }
          of_node_put(ver_np);
          return 0;
          }
          early_initcall(hmbird_patch_init);
          MODULE_LICENSE("GPL");
          MODULE_AUTHOR("reigadegr");
          MODULE_DESCRIPTION("Forcefully convert HMBIRD_OGKI to HMBIRD_GKI.");
          EOF
          
          if ! grep -q "hmbird_patch.o" Makefile; then
            echo "obj-y += hmbird_patch.o" >> Makefile
          fi
          echo "✅ HMBird GKI补丁应用完成"

      - name: Apply lz4kd Patches
        if: ${{ inputs.enable_feature_y }}
        run: |
          cd kernel_workspace/kernel_platform/common
          if [ -f "../../SukiSU_patch/other/zram/zram_patch/6.6/lz4kd.patch" ]; then
            cp ../../SukiSU_patch/other/zram/zram_patch/6.6/lz4kd.patch ./
            patch -p1 -F 3 < lz4kd.patch || echo "应用lz4kd patch失败"
          else
            echo "⚠️ lz4kd.patch文件不存在，跳过"
          fi
          echo "✅ lz4kd补丁应用完成"
           
      - name: Add SUSFS Configuration Settings
        run: |
          cd kernel_workspace/kernel_platform
          
          # 确保gki_defconfig文件存在
          if [ ! -f "./common/arch/arm64/configs/gki_defconfig" ]; then
            echo "❌ 错误：gki_defconfig文件不存在"
            find ./common -name "gki_defconfig" -type f
            exit 1
          fi
          
          echo "CONFIG_KSU=y" >> ./common/arch/arm64/configs/gki_defconfig

          # Add VFS configuration settings
          echo "CONFIG_KSU_SUSFS_SUS_SU=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_MANUAL_HOOK=y" >> ./common/arch/arm64/configs/gki_defconfig
          
          # Add SUSFS configuration settings
          echo "CONFIG_KSU_SUSFS=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_PATH=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_OVERLAYFS=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_TRY_UMOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_ENABLE_LOG=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y" >> ./common/arch/arm64/configs/gki_defconfig
          
          echo "CONFIG_CRYPTO_LZ4HC=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_CRYPTO_LZ4K=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_CRYPTO_LZ4KD=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_CRYPTO_842=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_LOCALVERSION_AUTO=n" >> ./common/arch/arm64/configs/gki_defconfig

          # Add BBR
          echo "CONFIG_TCP_CONG_ADVANCED=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_TCP_CONG_BBR=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_NET_SCH_FQ=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_TCP_CONG_BIC=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_TCP_CONG_CUBIC=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_TCP_CONG_WESTWOOD=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_TCP_CONG_HTCP=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_DEFAULT_TCP_CONG=bbr" >> ./common/arch/arm64/configs/gki_defconfig
          
          # Remove check_defconfig
          if [ -f "./common/build.config.gki" ]; then
            sed -i 's/check_defconfig//' ./common/build.config.gki || echo "修改build.config.gki失败"
          fi
          echo "✅ 配置设置完成"
      
      - name: Add KPM Configuration Settings
        if: ${{ inputs.enable_feature_x }}
        run: |
          cd kernel_workspace/kernel_platform
          echo "CONFIG_KPM=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "✅ KPM配置已添加"
          
      - name: Add make name
        run: |
          cd kernel_workspace/kernel_platform
          if [ -f "./common/scripts/setlocalversion" ]; then
            sed -i 's/${scm_version}//' ./common/scripts/setlocalversion
          fi
          if [ -f "./common/arch/arm64/configs/gki_defconfig" ]; then
            sed -i 's/-4k/${{ github.event.inputs.KERNEL_SUFFIX }}/g' ./common/arch/arm64/configs/gki_defconfig
          fi
          echo "✅ 内核名称设置完成"

      - name: Add sched_ext
        if: ${{ inputs.enable_feature_z }}
        run: |
          cd kernel_workspace/kernel_platform/
          git clone https://github.com/HanKuCha/sched_ext.git || echo "sched_ext已存在或克隆失败"
          if [ -d "sched_ext" ]; then
            cp -r ./sched_ext/* ./common/kernel/sched/ || echo "复制sched_ext文件失败"
          fi
          echo "✅ 风驰驱动添加完成"

      - name: Build Kernel
        run: |
          export KBUILD_BUILD_TIMESTAMP="${{ github.event.inputs.KERNEL_TIME }}"
          export PATH="$GITHUB_WORKSPACE/kernel_workspace/kernel_platform/prebuilts/clang/host/linux-x86/clang-r510928/bin:$PATH"
          export PATH="/usr/lib/ccache:$PATH"
          
          cd kernel_workspace/kernel_platform/common
          echo "开始构建内核..."
          make -j$(nproc --all) LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- CC=clang RUSTC=../../prebuilts/rust/linux-x86/1.73.0b/bin/rustc PAHOLE=../../prebuilts/kernel-build-tools/linux-x86/bin/pahole LD=ld.lld HOSTLD=ld.lld O=out KCFLAGS+=-O2 gki_defconfig all
          echo "✅ 内核构建完成"
          
      - name: Apply Linux Patch
        run: |
          cd kernel_workspace/kernel_platform/common/out/arch/arm64/boot
          if [ -f "Image" ]; then
            curl -LO https://github.com/SukiSU-Ultra/SukiSU_KernelPatch_patch/releases/download/0.12.0/patch_linux
            chmod +x patch_linux
            ./patch_linux
            rm -f Image
            mv oImage Image
            echo "✅ Linux补丁应用完成"
          else
            echo "❌ 错误：找不到内核Image文件"
            ls -la
            exit 1
          fi
         
      - name: Make AnyKernel3
        run: |
          git clone https://github.com/Xiaomichael/AnyKernel3.git --depth=1   
          rm -rf ./AnyKernel3/.git
          rm -f ./AnyKernel3/push.sh
          if [ -f "kernel_workspace/kernel_platform/common/out/arch/arm64/boot/Image" ]; then
            cp kernel_workspace/kernel_platform/common/out/arch/arm64/boot/Image ./AnyKernel3/
            echo "✅ AnyKernel3打包完成"
          else
            echo "❌ 错误：找不到内核Image文件"
            find kernel_workspace -name "Image" -type f
            exit 1
          fi

      - name: Upload AnyKernel3
        uses: actions/upload-artifact@v4
        with:
          name: SukiSU_oneplus_ace5_Ultra_${{ github.sha }}
          path: ./AnyKernel3/*
          
      - name: Build Success
        run: |
          echo "🎉 内核构建流程完成！"
          echo "出场代码: 2"

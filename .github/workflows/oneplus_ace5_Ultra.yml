name: oneplus_ace5_Ultra
on:
  workflow_dispatch:
    inputs:
      KERNEL_SUFFIX:
        description: '内核名称修改(可改中文和emoji)'
        required: true
        default: '-android15-oki-xiaoxiaow'
      KERNEL_TIME:
        description: "内核构建日期更改(默认为原厂)"
        required: true
        default: 'Tue Jul  1 19:48:18 UTC 2025'
      enable_feature_x:
        description: "是否启用kpm"
        required: true
        default: false
        type: boolean
      enable_feature_y:
        description: "是否启用lz4kd"
        required: true
        default: true
        type: boolean
      enable_feature_z:
        description: "是否添加风驰驱动"
        required: true
        default: true
        type: boolean

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"
      CCACHE_DIR: /home/runner/.ccache  
      CCACHE_MAXSIZE: 8G
    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          
      - name: Configure Git
        run: |
          git config --global user.name "Futaba"
          git config --global user.email "3199167602@qq.com"

      - name: Install dependencies
        run: |
          sudo apt update && sudo apt upgrade -y
          sudo apt install -y python3 git curl ccache libelf-dev build-essential flex bison libssl-dev libncurses-dev

      - name: Restore ccache
        uses: actions/cache@v3
        with:
          path: /home/runner/.ccache
          key: ccache-${{ runner.os }}-${{ github.ref }}
          restore-keys: |
            ccache-${{ runner.os }}-

      - name: Install repo tool
        run: |
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/repo
          chmod a+x ~/repo
          sudo mv ~/repo /usr/local/bin/repo

      - name: Initialize repo and sync
        run: |
          mkdir kernel_workspace && cd kernel_workspace
          repo init -u https://github.com/HanKuCha/kernel_manifest.git -b refs/heads/oneplus/sm8750 -m oneplus_ace5_Ultra.xml --depth=1
          repo --trace sync -c -j$(nproc --all) --no-tags
          rm -f kernel_platform/common/android/abi_gki_protected_exports_* || echo "No protected exports!"
          rm -f kernel_platform/msm-kernel/android/abi_gki_protected_exports_* || echo "No protected exports!"
         
      - name: Check kernel structure
        run: |
          cd kernel_workspace/kernel_platform
          echo "检查内核目录结构:"
          find . -name "Makefile" -type f | head -10
          ls -la
          echo "common目录内容:"
          ls -la common/
         
      - name: Set up SukiSU (修复版)
        run: |
          cd kernel_workspace/kernel_platform
          echo "设置SukiSU..."
          
          # 首先检查内核版本
          if [ -f "common/Makefile" ]; then
            cd common
            KERNEL_VERSION=$(head -n 5 Makefile | grep -E '^VERSION|^PATCHLEVEL' | sed 's/.*= //' | tr '\n' '.' | sed 's/\.$//')
            echo "检测到内核版本: $KERNEL_VERSION"
            cd ..
          else
            echo "使用默认内核版本: 6.6"
            KERNEL_VERSION="6.6"
          fi
          
          # 克隆KernelSU
          git clone https://github.com/tiann/KernelSU.git || echo "KernelSU已存在"
          cd KernelSU
          
          # 根据内核版本选择分支
          if [ "$KERNEL_VERSION" = "6.6" ] || [ "$KERNEL_VERSION" = "6.1" ]; then
            git checkout main || echo "使用main分支"
          else
            git checkout main || echo "使用main分支"
          fi
          
          echo "✅ SukiSU设置完成"
        
      - name: Set up KernelSU in kernel
        run: |
          cd kernel_workspace/kernel_platform
          echo "配置KernelSU到内核..."
          
          # 检查KernelSU工具是否可用
          if [ -d "KernelSU" ]; then
            cd KernelSU
            if [ -f "kernel_patch.sh" ]; then
              ./kernel_patch.sh ../common || echo "KernelSU补丁应用完成"
            else
              echo "⚠️ kernel_patch.sh不存在，手动配置"
            fi
          fi
          
          echo "✅ KernelSU内核配置完成"
      
      - name: Apply HMBird GKI patch
        run: |
          cd kernel_workspace/kernel_platform/common/drivers
          echo "应用HMBird GKI补丁..."
          
          # 创建补丁文件
          cat << 'EOF' > hmbird_patch.c
          #include <linux/init.h>
          #include <linux/module.h>
          #include <linux/of.h>
          #include <linux/slab.h>
          #include <linux/string.h>

          static int __init hmbird_patch_init(void)
          {
              struct device_node *ver_np;
              const char *type;
              int ret;

              ver_np = of_find_node_by_path("/soc/oplus,hmbird/version_type");
              if (!ver_np) {
                    pr_info("hmbird_patch: version_type node not found\n");
                    return 0;
              }

              ret = of_property_read_string(ver_np, "type", &type);
              if (ret) {
                    pr_info("hmbird_patch: type property not found\n");
                    of_node_put(ver_np);
                    return 0;
              }

              if (strcmp(type, "HMBIRD_OGKI")) {
                    of_node_put(ver_np);
                    return 0;
              }

              struct property *prop = of_find_property(ver_np, "type", NULL);
              if (prop) {
                    struct property *new_prop = kmalloc(sizeof(*prop), GFP_KERNEL);
                    if (!new_prop) {
                        pr_info("hmbird_patch: kmalloc for new_prop failed\n");
                        of_node_put(ver_np);
                        return 0;
              }
              memcpy(new_prop, prop, sizeof(*prop));
              new_prop->value = kmalloc(strlen("HMBIRD_GKI") + 1, GFP_KERNEL);
              if (!new_prop->value) {
                    pr_info("hmbird_patch: kmalloc for new_prop->value failed\n");
                    kfree(new_prop);
                    of_node_put(ver_np);
                    return 0;
              }
              strcpy(new_prop->value, "HMBIRD_GKI");
              new_prop->length = strlen("HMBIRD_GKI") + 1;

              if (of_remove_property(ver_np, prop) != 0) {
                  pr_info("hmbird_patch: of_remove_property failed\n");
                  return 0;
              }
              if (of_add_property(ver_np, new_prop) !=0) {
                  pr_info("hmbird_patch: of_add_property failed\n");
                  return 0;
              }
              pr_info("hmbird_patch: success from HMBIRD_OGKI to HMBIRD_GKI\n");
          }
          else {
              pr_info("hmbird_patch: type property structure not found\n");
          }
          of_node_put(ver_np);
          return 0;
          }
          early_initcall(hmbird_patch_init);
          MODULE_LICENSE("GPL");
          MODULE_AUTHOR("reigadegr");
          MODULE_DESCRIPTION("Forcefully convert HMBIRD_OGKI to HMBIRD_GKI.");
          EOF
          
          # 添加到Makefile
          if ! grep -q "hmbird_patch.o" Makefile; then
            echo "# HMBird GKI Patch" >> Makefile
            echo "obj-y += hmbird_patch.o" >> Makefile
          fi
          echo "✅ HMBird GKI补丁应用完成"

      - name: Clean and prepare configuration
        run: |
          cd kernel_workspace/kernel_platform/common
          echo "准备内核配置..."
          
          # 确保使用正确的defconfig
          if [ -f "arch/arm64/configs/vendor/sm8750_GKI.config" ]; then
            cp arch/arm64/configs/vendor/sm8750_GKI.config arch/arm64/configs/gki_defconfig
            echo "使用sm8750_GKI.config作为基础配置"
          elif [ -f "arch/arm64/configs/gki_defconfig" ]; then
            echo "使用现有的gki_defconfig"
          else
            echo "❌ 错误：找不到defconfig文件"
            find arch/arm64/configs -name "*.config" -type f
            exit 1
          fi
          
          echo "✅ 配置准备完成"
           
      - name: Add KernelSU configuration
        run: |
          cd kernel_workspace/kernel_platform
          echo "添加KernelSU配置..."
          
          if [ ! -f "./common/arch/arm64/configs/gki_defconfig" ]; then
            echo "❌ 错误：gki_defconfig文件不存在"
            exit 1
          fi
          
          # KernelSU 基本配置
          echo "CONFIG_KSU=y" >> ./common/arch/arm64/configs/gki_defconfig
          
          echo "✅ KernelSU配置添加完成"
      
      - name: Add basic kernel configuration
        run: |
          cd kernel_workspace/kernel_platform
          echo "添加基础内核配置..."
          
          # 基础配置
          echo "CONFIG_LOCALVERSION_AUTO=n" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_IKCONFIG=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_IKCONFIG_PROC=y" >> ./common/arch/arm64/configs/gki_defconfig

          # 网络优化
          echo "CONFIG_TCP_CONG_BBR=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_NET_SCH_FQ=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_DEFAULT_TCP_CONG=bbr" >> ./common/arch/arm64/configs/gki_defconfig
          
          echo "✅ 基础配置设置完成"
      
      - name: Add KPM Configuration
        if: ${{ inputs.enable_feature_x }}
        run: |
          cd kernel_workspace/kernel_platform
          echo "CONFIG_MODULES=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KPM=y" >> ./common/arch/arm64/configs/gki_defconfig
          echo "✅ KPM配置已添加"
          
      - name: Set kernel name and version
        run: |
          cd kernel_workspace/kernel_platform
          echo "设置内核名称和版本..."
          
          # 修改setlocalversion脚本
          if [ -f "./common/scripts/setlocalversion" ]; then
            sed -i 's/${scm_version}//' ./common/scripts/setlocalversion
            sed -i 's/res.*//' ./common/scripts/setlocalversion
          fi
          
          # 添加本地版本
          echo 'CONFIG_LOCALVERSION="${{ github.event.inputs.KERNEL_SUFFIX }}"' >> ./common/arch/arm64/configs/gki_defconfig
          
          echo "✅ 内核名称设置完成"

      - name: Prepare build environment
        run: |
          cd kernel_workspace/kernel_platform/common
          echo "准备构建环境..."
          
          # 设置环境变量
          export ARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export LLVM=1
          export CC=clang
          export LD=ld.lvd
          
          echo "✅ 构建环境准备完成"

      - name: Build Kernel (简化版)
        run: |
          cd kernel_workspace/kernel_platform/common
          echo "开始构建内核..."
          
          # 设置环境变量
          export KBUILD_BUILD_TIMESTAMP="${{ github.event.inputs.KERNEL_TIME }}"
          export PATH="$GITHUB_WORKSPACE/kernel_workspace/kernel_platform/prebuilts/clang/host/linux-x86/clang-r510928/bin:$PATH"
          export PATH="/usr/lib/ccache:$PATH"
          export ARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export LLVM=1
          export CC=clang
          export LD=ld.lld
          
          echo "步骤1: 生成配置..."
          make O=out LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- CC=clang gki_defconfig
          
          echo "步骤2: 查看配置差异..."
          make O=out LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- CC=clang olddefconfig
          
          echo "步骤3: 开始编译内核..."
          make -j$(nproc --all) O=out LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- CC=clang 2>&1 | tee build.log
          
          BUILD_RESULT=${PIPESTATUS[0]}
          if [ $BUILD_RESULT -eq 0 ]; then
            echo "✅ 内核构建成功完成"
          else
            echo "❌ 内核构建失败，错误代码: $BUILD_RESULT"
            echo "最后50行构建日志:"
            tail -50 build.log
            exit $BUILD_RESULT
          fi
          
      - name: Check build output
        run: |
          cd kernel_workspace/kernel_platform/common
          echo "检查构建输出..."
          
          if [ -f "out/arch/arm64/boot/Image" ]; then
            echo "✅ 内核Image文件生成成功"
            ls -lh out/arch/arm64/boot/Image
            file out/arch/arm64/boot/Image
          else
            echo "❌ 错误：内核Image文件未生成"
            echo "查找可能的输出文件:"
            find out -name "Image" -type f
            find out -name "*.gz" -type f
            find out -name "vmlinux" -type f
            exit 1
          fi
         
      - name: Apply kernel patch if available
        run: |
          cd kernel_workspace/kernel_platform/common/out/arch/arm64/boot
          if [ -f "Image" ]; then
            echo "尝试应用内核补丁..."
            if curl -f -LO https://github.com/SukiSU-Ultra/SukiSU_KernelPatch_patch/releases/download/0.12.0/patch_linux; then
              chmod +x patch_linux
              ./patch_linux
              if [ -f "oImage" ]; then
                rm -f Image
                mv oImage Image
                echo "✅ Linux补丁应用完成"
              else
                echo "⚠️ oImage未生成，使用原始Image"
              fi
            else
              echo "⚠️ 无法下载patch_linux，使用原始Image"
            fi
          else
            echo "❌ 错误：找不到内核Image文件"
            exit 1
          fi
         
      - name: Create AnyKernel3 package
        run: |
          echo "制作AnyKernel3刷机包..."
          git clone https://github.com/Xiaomichael/AnyKernel3.git --depth=1   
          rm -rf ./AnyKernel3/.git
          rm -f ./AnyKernel3/push.sh
          
          if [ -f "kernel_workspace/kernel_platform/common/out/arch/arm64/boot/Image" ]; then
            cp kernel_workspace/kernel_platform/common/out/arch/arm64/boot/Image ./AnyKernel3/
            
            # 更新anykernel.sh脚本
            cat << 'EOF' > ./AnyKernel3/anykernel.sh
            #!/bin/bash
            # AnyKernel3 Script for OnePlus Ace 5 Ultra
            
            ## 内核属性
            kernel.string=SukiSU Kernel for OnePlus Ace 5 Ultra
            do.devicecheck=1
            do.modules=0
            do.systemless=1
            do.cleanup=1
            do.cleanuponabort=0
            device.name1=oneplus_ace5_ultra
            
            ## 安装过程
            dump_boot() {
              ui_print " "
              ui_print "正在备份boot分区..."
            }
            
            split_boot() {
              ui_print "正在解包boot镜像..."
            }
            
            patch_dtbo() {
              ui_print "正在修补dtbo..."
            }
            
            flash_boot() {
              ui_print "正在刷入内核..."
              ui_print " "
              ui_print "SukiSU Kernel 刷入完成!"
            }
            EOF
            
            echo "✅ AnyKernel3刷机包制作完成"
            ls -la ./AnyKernel3/
          else
            echo "❌ 错误：找不到内核Image文件"
            exit 1
          fi

      - name: Upload Kernel Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SukiSU_Kernel_OnePlus_Ace5_Ultra
          path: ./AnyKernel3/*
          
      - name: Build Success
        run: |
          echo "🎉 内核构建成功完成！"
          echo "构建时间: $(date)"
          echo "内核文件: SukiSU_Kernel_OnePlus_Ace5_Ultra.zip"
          echo "出场代码: 0"
